# Copy this file to `.dev.vars` and fill in real values for local `wrangler dev`.
# DO NOT commit `.dev.vars` to git.
#
# In production, set these via:
#   wrangler secret put OPENAI_API_KEY
#   wrangler secret put APP_PIN

OPENAI_API_KEY="sk-..."
APP_PIN="pick-a-long-random-string-16-chars-min"
